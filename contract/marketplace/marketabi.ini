registerMarketItem(tokenContract, tokenId, saleType, buyNowPrice, minimumBiddingPrice,currency, openTimeUtc, closeTimeUtc)
isManager()
transferFrom(to, nft, tokenID) 
cancelMarketItem(marketId)
buyNowWithToken(marketId, amount, currency)
submitBiddingTransactionWithSeller(marketId, buyer, amount)

confirmBiddingTransactionWithBuyer(marketId, amount)
setMarketFee(newFee)
setRoyaltyFee(newFee)
setMinterRoyaltyPercentage(newPercentage)
setMandatoryMarketDataContract(marketData)
approveFeesForERC20Token(currency)
collectFees(currency)

getMarketDataContractAddress()
getMarketFee()
getRoyaltyFee()
getMinterRoyaltyPercentage()


_buyNow(marketId, amount, currency)
_settle(marketItem, buyerAddress, amount, currency)
_calcRoyaltyFee(amount)
_calcMarketFee(amount)